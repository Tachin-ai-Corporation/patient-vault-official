# GET /v2/user/myself

Retrieve the profile and organization context for the authenticated user.

**Authentication**: Bearer JWT required.

---

## GET /v2/user/myself

Returns the current user's identity, tenant, and permissions used by Patient Vault.

### Response

#### 200

The authenticated user's profile and organization context.
